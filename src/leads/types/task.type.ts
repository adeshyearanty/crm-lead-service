export enum TaskStatus {
  PENDING = 'Pending',
  COMPLETED = 'Completed',
  OVERDUE = 'Overdue',
}

export enum TaskType {
  CALL = 'Call',
  EMAIL = 'Email',
  MEETING = 'Meeting',
  REMINDER = 'Reminder',
  OTHER = 'Other',
}

export enum TaskPriority {
  HIGH = 'High',
  MEDIUM = 'Medium',
  LOW = 'Low',
}
